{
  "name": "stylist-ai-proxy",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Прокси к Claude (через OpenRouter) для ИИ-стилиста «Стиль». Держит ключ на сервере, без npm-зависимостей.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
